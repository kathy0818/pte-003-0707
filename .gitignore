# LaTeX 编译中间产物
*.aux
*.log
*.out
*.toc
*.synctex.gz

# build/paper = 可重新生成的工作台：预览 PDF 与临时图片目录不入库
build/paper/preview_*.pdf
build/paper/img/

# build/paper/frag_*.tex = workflow 并行起草用的临时片段，组装进 sec_<part>_body.tex 后即删除，不入库
build/paper/frag_*.tex

# build/zoom = 读图时从裁剪素材再裁切的临时放大图，可随时重生成，不入库
build/zoom/

# 成品 PDF（整理卷/）要入库，不在此忽略
